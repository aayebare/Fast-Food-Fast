Admin returns a single order.
---
tags:
 - Admin returns a specific order made
parameters:
  - in: body
    name: get single order
    required: true
    description: Endpoint for admin to return a specific order made by a user
responses:
  200:
    description: Order successfully returned
    
  401:
    description: Access not granted to non admin
security:
  - JWT:
      description: takes in a jwt token
      name: Authorization
      in: header
      bearerFormat: JWT      
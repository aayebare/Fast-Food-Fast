Order status update.
---
tags:
 - Admin updates the status of an order
parameters:
  - in: body
    name: status update
    required: true
    description: Endpoint for admin to update the status of an order
responses:
  200:
    description: Status successfully updated
    schema:
      id: update_order_status
      properties:
        order_status:
          type: string
          default: new
        
  400:
      description: Update status with un accepted values 
security:
  - JWT:
      description: takes in a jwt token
      name: Authorization
      in: header
      bearerFormat: JWT        

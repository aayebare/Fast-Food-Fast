Admin gets all orders.
---
tags:
 - Admin returns all orders made
parameters:
  - in: body
    name: get all orders
    required: true
    description: Endpoint for admin to get all orders from all users
responses:
  200:
    description: All orders successfully returned
    
  401:
    description: Access not granted to non admin
security:
  - JWT:
      description: takes in a jwt token
      name: Authorization
      in: header
      bearerFormat: JWT      